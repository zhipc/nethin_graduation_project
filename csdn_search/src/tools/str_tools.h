#ifndef __STR_TOOLS__
#define __STR_TOOLS__

#include <stddef.h>
#include <string.h>

char* trim(const char* str);

char* r_trim(const char* str);
char* l_trim(const char* str);

#endif	/* __STR__TOOLS */
